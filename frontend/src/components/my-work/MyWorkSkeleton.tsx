import { DashboardGridLoading } from "./DashboardGrid";

/** Full-page placeholder — same grid cells and container as the loaded page. */
export function MyWorkSkeleton() {
  return <DashboardGridLoading />;
}
